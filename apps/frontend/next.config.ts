import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: [
    "genitival-paleoentomologic-adalberto.ngrok-free.dev",
    "http://localhost:3001",
  ],
};

export default nextConfig;
